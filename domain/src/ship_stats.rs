#[derive(Debug, Clone)]
pub struct ShipStats {
    shield_hp: usize,
    armor_hp: usize,
    hull_hp: usize,
    velocity: usize,
    agility: usize,
    mass: usize,
    power_grid: usize,
    cpu: usize,
}
